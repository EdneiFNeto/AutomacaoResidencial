import React from 'react';
import {StatusBar} from 'react-native';

const StatusBarComponent: React.FC = () => {
  return <StatusBar animated={true} backgroundColor="#39a5c7" hidden={false} />;
};

export default StatusBarComponent;
